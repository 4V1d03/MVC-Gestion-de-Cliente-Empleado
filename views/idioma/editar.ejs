<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Idioma</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
</head>
<body>

    <div class="container mt-5">
        <h2 class="text-center">Agregar Nuevo Idioma</h2>

        <div class="card p-4 shadow-lg">
            <form action="/edit3" method="post">
                <div class="row">
                    
                    <div class="col-md-6 mb-3">
                        <label class="form-label"><i class="bi bi-hash"></i> Código Idioma</label>
                        <input type="text" name="codigoIdioma" class="form-control" value="<%= idiomas.codigoIdioma %>" readonly>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label class="form-label"><i class="bi bi-hash"></i> Código Empleado</label>
                        <input type="text" name="codigoEmp" class="form-control" value="<%= idiomas.codigoEmp %>" readonly>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label class="form-label"><i class="bi bi-translate"></i> Idioma</label>
                        <input type="text" name="nombreIdioma" class="form-control" value="<%= idiomas.nombreIdioma %>" required>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label class="form-label"><i class="bi bi-clock"></i> Tiempo de Aprendizaje (años)</label>
                        <input type="number" name="tiempo" class="form-control" value="<%= idiomas.tiempo %>" min="0" required>
                    </div>
                </div>

                <div class="text-center">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-plus-circle"></i> Editar Idioma
                    </button>
                    <a href="/Idioma" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Cancelar
                    </a>
                </div>
            </form>
        </div>
    </div>

</body>
</html>